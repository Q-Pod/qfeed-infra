alloy:
  configMap:
    create: false
    name: config-alloy-daemonset
    key: config.alloy

  mounts:
    varlog: false
    dockercontainers: false
    extra:
      - name: host-proc
        mountPath: /host/proc
        readOnly: true
      - name: host-sys
        mountPath: /host/sys
        readOnly: true

  extraEnv:
    - name: NODE_NAME
      valueFrom:
        fieldRef:
          fieldPath: spec.nodeName

controller:
  type: daemonset
  volumes:
    extra:
      - name: host-proc
        hostPath:
          path: /proc
      - name: host-sys
        hostPath:
          path: /sys

serviceAccount:
  create: true
  name: alloy-daemonset

rbac:
  create: true
